<%= form_for(@admission_form) do |f| %>
  <% if @admission_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admission_form.errors.count, "error") %> prohibited this admission_form from being saved:</h2>

      <ul>
      <% @admission_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
